{
  "name": "node-vitest-practice",
  "version": "1.0.0",
  "description": "Simple Node.js project using Vitest and Yarn",
  "type": "module",
  "scripts": {
    "test": "vitest --run --reporter verbose --environment happy-dom",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "happy-dom": "^17.4.7",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}
